import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.purpose.technology',
  appName: 'Purpose Technology',
  webDir: 'out',
  // If you prefer loading the deployed site instead of bundling local assets,
  // uncomment and set your production URL below, then run `npx cap sync`.
  // server: {
  //   url: 'https://your-vercel-domain.vercel.app',
  //   cleartext: false,
  // },
};

export default config;
